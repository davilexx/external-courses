const addNewProp = (str, obj) => {
  const newObject = obj;
  Object.keys(obj).forEach((key) => {
    if (key !== str) {
      newObject[str] = 'new';
    }
  });
  return newObject;
};

module.exports = addNewProp;
